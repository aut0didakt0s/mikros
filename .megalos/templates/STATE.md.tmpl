# mikrós state

active_milestone:
active_slice:
active_task:
active_worktree:
active_worktree_path:
loc_budget: 300

## Recently completed

(none)

## Notes

This file is the single source of truth for "where am I in the workflow?" Every mikrós command reads it first and writes it last (atomically, via temp file + mv).
